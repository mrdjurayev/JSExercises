const name = prompt("Enter your variable for name"); //And the variable is created like this.
const age = 20;
this[name] = age;

console.log(myAge); // Display the entered result in the console using its name.